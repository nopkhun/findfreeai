---
phase: 01-architecture-reset-and-deployment-simplification
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - app.py
  - dashboard.py
  - proxy.py
  - docs/DASHBOARD_OPERATIONS.md
  - tests/smoke/test_openai_compat.py
  - tests/smoke/test_routing_fallback.py
  - scripts/verify_phase1.py
  - README.md
autonomous: true
requirements_addressed:
  - DASH-01
  - DASH-02
  - API-01
  - QUAL-01
requirements:
  - DASH-01
  - DASH-02
  - API-01
  - QUAL-01
must_haves:
  truths:
    - "Dashboard clearly shows provider health, config status, and critical warnings for fast troubleshooting."
    - "Operator-facing dashboard text and logs remain Thai-first after refactor."
    - "OpenClaw-compatible OpenAI endpoints remain functional for models listing and chat completions."
    - "A repeatable verification flow confirms routing and failover readiness before release."
  artifacts:
    - "app.py/dashboard.py render provider-health + config-warning panels."
    - "proxy.py preserves `/v1/models` and `/v1/chat/completions` compatibility behavior."
    - "tests/smoke/* contains executable compatibility + fallback smoke tests."
    - "scripts/verify_phase1.py runs the full checklist and exits non-zero on failures."
  key_links:
    - "Dashboard consumes proxy `/v1/providers` and `/v1/config` status for UI panels."
    - "Smoke tests hit live proxy endpoints and assert OpenAI-compatible response shapes."
    - "Phase verification script orchestrates smoke tests + deployment readiness checks from one command."
---

<objective>
Deliver operator UX and compatibility assurance needed to ship Phase 1 safely.

Purpose: Ensure troubleshooting visibility, Thai-first messaging, and OpenClaw API compatibility with a single verification gate.
Output: Improved operational dashboard + compatibility smoke tests + one executable phase verification command.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@app.py
@dashboard.py
@proxy.py
@README.md
</context>

<tasks>

<task id="P03-T01" title="Upgrade dashboard operational visibility panels">
  <name>Upgrade dashboard operational visibility panels</name>
  <read_first>
    - app.py
    - dashboard.py
    - proxy.py
    - .planning/phases/01-architecture-reset-and-deployment-simplification/01-CONTEXT.md
  </read_first>
  <files>
    - app.py
    - dashboard.py
    - docs/DASHBOARD_OPERATIONS.md
  </files>
  <action>
    Add/adjust dashboard sections to highlight provider health summary, key configuration status (masked), and actionable warnings with clear remediation guidance (DASH-01).
    Source these views from live proxy endpoints (`/v1/providers`, `/v1/config`, `/v1/stats`) and avoid static placeholder data.
    Document dashboard troubleshooting map in docs/DASHBOARD_OPERATIONS.md.
  </action>
  <verify>
    - rg "สถานะผู้ให้บริการ|สถานะคีย์และคอนฟิก|คำแนะนำการแก้ไข" app.py dashboard.py
    - rg "v1/providers|v1/config|v1/stats" app.py dashboard.py docs/DASHBOARD_OPERATIONS.md
  </verify>
  <done>
    Dashboard exposes concrete operational panels backed by live proxy status endpoints.
  </done>
  <acceptance_criteria>
    Dashboard UI includes visible sections with exact titles `สถานะผู้ให้บริการ`, `สถานะคีย์และคอนฟิก`, and `คำแนะนำการแก้ไข`; provider-down state is rendered from `/v1/providers` data with a badge containing `DOWN`; missing-key state is rendered from config/status data with a warning containing `ยังไม่ได้ตั้งค่า`; `docs/DASHBOARD_OPERATIONS.md` maps each panel title to its source endpoint and remediation action.
  </acceptance_criteria>
</task>

<task id="P03-T02" title="Enforce Thai-first operator language and preserve OpenAI compatibility">
  <name>Enforce Thai-first operator language and preserve OpenAI compatibility</name>
  <read_first>
    - app.py
    - dashboard.py
    - proxy.py
    - README.md
  </read_first>
  <files>
    - app.py
    - dashboard.py
    - proxy.py
    - README.md
  </files>
  <action>
    Audit and normalize operator-facing text/log output to Thai-first phrasing across dashboard/proxy startup and error surfaces (DASH-02).
    Preserve and, if needed, harden `/v1/models` and `/v1/chat/completions` schema/behavior for OpenClaw integration (API-01), including streaming and non-streaming paths.
    Update README compatibility section with explicit sample requests/responses for these two endpoints.
  </action>
  <verify>
    - python scripts/verify_phase1.py --string-audit-only
    - curl -s http://127.0.0.1:8900/v1/models
    - curl -s -X POST http://127.0.0.1:8900/v1/chat/completions -H "Content-Type: application/json" -d '{"model":"auto","messages":[{"role":"user","content":"ทดสอบ"}]}'
  </verify>
  <done>
    Thai-first operator text is enforced while required OpenAI-compatible endpoints stay stable.
  </done>
  <acceptance_criteria>
    `app.py`, `dashboard.py`, and `proxy.py` expose operator-facing labels/errors in Thai for startup, health, and warning surfaces (verified by string audit script output listing zero English-only operator labels); `curl -s http://127.0.0.1:8900/v1/models` returns JSON with top-level key `data` as array; `POST /v1/chat/completions` with model `auto` returns JSON containing keys `id`, `object`, `choices`, `model` (non-streaming) and emits `data:` events when `stream=true`.
  </acceptance_criteria>
</task>

<task id="P03-T03" title="Build executable Phase-1 verification suite">
  <name>Build executable Phase-1 verification suite</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - proxy.py
    - docker-compose.yml
  </read_first>
  <files>
    - tests/smoke/test_openai_compat.py
    - tests/smoke/test_routing_fallback.py
    - scripts/verify_phase1.py
  </files>
  <action>
    Create smoke tests in tests/smoke/test_openai_compat.py and tests/smoke/test_routing_fallback.py to validate endpoint compatibility, routing behavior, and provider fallback (QUAL-01).
    Add scripts/verify_phase1.py to run deployment readiness checks + smoke tests and print a requirement-by-requirement pass/fail summary.
    Ensure script exits non-zero if any critical verification fails so it can gate releases.
  </action>
  <verify>
    - python scripts/verify_phase1.py --mode local
    - pytest tests/smoke/test_openai_compat.py -q
    - pytest tests/smoke/test_routing_fallback.py -q
  </verify>
  <done>
    Phase verification is runnable as one deterministic command and can gate release on failures.
  </done>
  <acceptance_criteria>
    One command executes the full phase checklist and produces deterministic pass/fail output for API compatibility, fallback behavior, and deployment readiness.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- python scripts/verify_phase1.py --mode local
- pytest tests/smoke/test_openai_compat.py -q
- pytest tests/smoke/test_routing_fallback.py -q
- curl -s http://127.0.0.1:8900/v1/models
- curl -s -X POST http://127.0.0.1:8900/v1/chat/completions -H "Content-Type: application/json" -d '{"model":"auto","messages":[{"role":"user","content":"ทดสอบ"}]}'
</verification>

<success_criteria>
- DASH-01/02 met via operational visibility and Thai-first UX consistency.
- API-01 met via validated endpoint compatibility for OpenClaw.
- QUAL-01 met via executable, repeatable verification suite.
</success_criteria>
