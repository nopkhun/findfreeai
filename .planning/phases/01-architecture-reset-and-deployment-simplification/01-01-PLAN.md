---
phase: 01-architecture-reset-and-deployment-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/ARCHITECTURE.md
  - settings.py
  - proxy.py
  - app.py
  - .env.example
  - .gitignore
  - scripts/secret_guard.py
autonomous: true
requirements_addressed:
  - ARCH-01
  - ARCH-02
  - CONF-01
  - CONF-02
  - SEC-01
requirements:
  - ARCH-01
  - ARCH-02
  - CONF-01
  - CONF-02
  - SEC-01
must_haves:
  truths:
    - "Maintainer sees one canonical runtime architecture with explicit service boundaries."
    - "Startup fails fast with Thai-readable diagnostics when required config is missing/invalid."
    - "Secret handling is enforced by code and repo guardrails, not only tribal knowledge."
  artifacts:
    - "docs/ARCHITECTURE.md defines service ownership, startup order, and data paths."
    - "settings.py provides centralized env schema + validation helpers used by runtimes."
    - "scripts/secret_guard.py checks staged files for obvious secrets before commit."
  key_links:
    - "proxy.py imports settings.py validation before binding network port."
    - "app.py imports same validation contract (single config truth source)."
    - "README.md references docs/ARCHITECTURE.md as canonical architecture source."
---

<objective>
Establish a single architecture/configuration contract so all runtimes and docs align.

Purpose: Remove conflicting guidance and make invalid configuration fail immediately with clear Thai messages.
Output: Canonical architecture doc + centralized settings validation + secret safety guardrails.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-architecture-reset-and-deployment-simplification/01-CONTEXT.md
@.planning/phases/01-architecture-reset-and-deployment-simplification/01-RESEARCH.md
@README.md
@proxy.py
@app.py
</context>

<tasks>

<task id="P01-T01" title="Publish canonical runtime architecture contract">
  <name>Publish canonical runtime architecture contract</name>
  <read_first>
    - .planning/phases/01-architecture-reset-and-deployment-simplification/01-CONTEXT.md
    - README.md
    - docker-compose.yml
    - run_all.py
  </read_first>
  <files>
    - docs/ARCHITECTURE.md
    - README.md
  </files>
  <action>
    Create docs/ARCHITECTURE.md and update README.md to point to it as canonical runtime guidance (ARCH-01, ARCH-02).
    Include: service ownership boundaries (proxy/dashboard/frontend/memory), startup contracts, data paths, and exact startup sequence per mode.
    Mark any legacy/optional paths explicitly as non-canonical to remove ambiguity.
  </action>
  <verify>
    - rg "^## Canonical Runtime|^## Local Mode|^## Docker Mode|^## Coolify Mode|^## Service Boundaries" docs/ARCHITECTURE.md
    - rg "Canonical architecture: docs/ARCHITECTURE.md" README.md
  </verify>
  <done>
    Architecture source of truth is published and README points to it with one canonical reference.
  </done>
  <acceptance_criteria>
    `docs/ARCHITECTURE.md` exists and contains headings `## Canonical Runtime`, `## Local Mode`, `## Docker Mode`, `## Coolify Mode`, and `## Service Boundaries`; `README.md` contains exactly one line with `Canonical architecture: docs/ARCHITECTURE.md` and does not contain the phrase `legacy architecture` outside a dedicated `## Legacy / Optional Paths` section.
  </acceptance_criteria>
</task>

<task id="P01-T02" title="Implement centralized env schema and fail-fast validation">
  <name>Implement centralized env schema and fail-fast validation</name>
  <read_first>
    - proxy.py
    - app.py
    - .env.example
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    - settings.py
    - proxy.py
    - app.py
    - .env.example
  </files>
  <action>
    Add settings.py as the single configuration contract (CONF-01, CONF-02) with: required env list by mode (local/docker/coolify), typed parsing, and Thai diagnostic messages.
    Wire proxy.py and app.py startup to call settings validation before serving requests; fail with non-zero exit and readable error summary when required values are missing/invalid.
    Update .env.example to reflect the exact contract in settings.py (no drift).
  </action>
  <verify>
    - python -c "import settings; print('settings-ok')"
    - python proxy.py
    - python app.py
  </verify>
  <done>
    Runtime configuration is validated centrally and startup behavior is fail-fast with Thai diagnostics.
  </done>
  <acceptance_criteria>
    Running proxy or dashboard with missing required env exits early and prints Thai diagnostics naming missing vars; valid config starts successfully in all modes.
  </acceptance_criteria>
</task>

<task id="P01-T03" title="Enforce secrets safety baseline in repo workflows">
  <name>Enforce secrets safety baseline in repo workflows</name>
  <read_first>
    - .gitignore
    - README.md
    - .planning/phases/01-architecture-reset-and-deployment-simplification/01-CONTEXT.md
  </read_first>
  <files>
    - .gitignore
    - scripts/secret_guard.py
    - README.md
    - docs/ARCHITECTURE.md
  </files>
  <action>
    Harden .gitignore secret exclusions and add scripts/secret_guard.py to scan staged changes for key/token patterns (SEC-01).
    Document mandatory pre-commit command in README and architecture docs.
    Ensure forbidden files (.env, api_keys.json, credentials*) are explicitly listed and never added to tracked files.
  </action>
  <verify>
    - python scripts/secret_guard.py --staged
    - rg "\.env|api_keys\.json|credentials" .gitignore README.md docs/ARCHITECTURE.md
  </verify>
  <done>
    Secret guardrails are enforceable from both tooling and documented repository policy.
  </done>
  <acceptance_criteria>
    Secret guard script flags obvious keys/tokens in staged diff and exits non-zero; forbidden secret files are documented and ignored.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- python scripts/secret_guard.py --staged
- python -c "import settings; print('settings-ok')"
- python proxy.py (with intentionally missing required env) returns non-zero + Thai diagnostics
- python app.py (with intentionally missing required env) returns non-zero + Thai diagnostics
</verification>

<success_criteria>
- ARCH-01/02 satisfied via canonical architecture and explicit startup contracts.
- CONF-01/02 satisfied via one env schema and fail-fast startup behavior.
- SEC-01 satisfied via enforceable secret guardrails and ignore policy.
</success_criteria>
