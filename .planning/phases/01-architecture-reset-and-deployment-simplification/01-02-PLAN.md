---
phase: 01-architecture-reset-and-deployment-simplification
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - docker-compose.yml
  - scripts/run_local.py
  - scripts/ops.py
  - scripts/healthcheck.py
  - scripts/coolify_verify.sh
  - README.md
  - docs/DEPLOYMENT.md
autonomous: true
requirements_addressed:
  - DEP-01
  - DEP-02
  - DEP-03
  - OPS-01
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
  - OPS-01
must_haves:
  truths:
    - "Operator can run each deployment mode through one documented command path."
    - "Docker deployment exposes health checks and persistent data that survive restart."
    - "Coolify deployment has explicit env/domain checklist with post-deploy smoke verification."
    - "Operator can run restart/health/log commands consistently across modes."
  artifacts:
    - "scripts/run_local.py provides one local launcher with preflight checks."
    - "docker-compose.yml includes healthcheck + named volumes for persistent state."
    - "scripts/ops.py provides restart/health/log subcommands."
    - "docs/DEPLOYMENT.md contains local/docker/coolify runbooks and verification commands."
  key_links:
    - "README.md run commands call scripts/run_local.py and scripts/ops.py (not ad-hoc)."
    - "scripts/healthcheck.py validates proxy endpoints used in deployment docs."
    - "scripts/coolify_verify.sh uses same endpoint contract as proxy API smoke tests."
---

<objective>
Unify operational entrypoints and deployment runbooks for Local, Docker, and Coolify.

Purpose: Make deploy/restart/health troubleshooting repeatable for maintainers without hidden steps.
Output: Executable scripts + synchronized docs for all three deployment modes.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@README.md
@docker-compose.yml
@run_all.py
@proxy.py
</context>

<tasks>

<task id="P02-T01" title="Create canonical local run path with preflight checks">
  <name>Create canonical local run path with preflight checks</name>
  <read_first>
    - run_all.py
    - app.py
    - proxy.py
    - .planning/phases/01-architecture-reset-and-deployment-simplification/01-RESEARCH.md
  </read_first>
  <files>
    - scripts/run_local.py
    - README.md
    - docs/DEPLOYMENT.md
  </files>
  <action>
    Replace legacy multi-process ambiguity by adding scripts/run_local.py as the only local entrypoint (DEP-01).
    Implement preflight checks for required binaries, env validity (reuse settings.py contract from 01-01), writable data paths, and port conflicts before launch.
    Update README + docs/DEPLOYMENT.md with one local command and expected healthy output.
  </action>
  <verify>
    - python scripts/run_local.py --preflight-only
    - rg "python scripts/run_local.py" docs/DEPLOYMENT.md README.md
  </verify>
  <done>
    Local mode has one documented executable entrypoint with deterministic preflight behavior.
  </done>
  <acceptance_criteria>
    Running `python scripts/run_local.py --preflight-only` exits `0` on valid setup and prints lines containing `PRECHECK: python`, `PRECHECK: env`, `PRECHECK: ports`; on invalid setup it exits non-zero and prints at least one Thai error line prefixed with `ข้อผิดพลาด:`; `docs/DEPLOYMENT.md` includes a `## Local` section whose command block contains the exact command `python scripts/run_local.py`.
  </acceptance_criteria>
</task>

<task id="P02-T02" title="Standardize Docker and Coolify deployment contracts">
  <name>Standardize Docker and Coolify deployment contracts</name>
  <read_first>
    - docker-compose.yml
    - README.md
    - .planning/phases/01-architecture-reset-and-deployment-simplification/01-CONTEXT.md
  </read_first>
  <files>
    - docker-compose.yml
    - docs/DEPLOYMENT.md
    - scripts/coolify_verify.sh
  </files>
  <action>
    Refine docker-compose.yml for explicit health checks, persistent named volumes, and stable env defaults (DEP-02).
    Add docs/DEPLOYMENT.md Coolify section + scripts/coolify_verify.sh for domain/env checklist and post-deploy smoke test (DEP-03).
    Ensure runbook commands are copy-paste executable and align with compose labels/network assumptions.
  </action>
  <verify>
    - docker compose up -d && docker compose ps
    - bash scripts/coolify_verify.sh https://$DOMAIN_PROXY
  </verify>
  <done>
    Docker and Coolify deploy paths are aligned with health checks and explicit verification commands.
  </done>
  <acceptance_criteria>
    Docker restart preserves required runtime data; health check transitions to healthy; Coolify runbook has explicit env/domain prerequisites and a scriptable smoke test.
  </acceptance_criteria>
</task>

<task id="P02-T03" title="Provide unified operations command surface">
  <name>Provide unified operations command surface</name>
  <read_first>
    - README.md
    - docker-compose.yml
    - proxy.py
  </read_first>
  <files>
    - scripts/ops.py
    - scripts/healthcheck.py
    - README.md
    - docs/DEPLOYMENT.md
  </files>
  <action>
    Implement scripts/ops.py with subcommands for restart, health, and logs across local/docker/coolify modes (OPS-01).
    Add scripts/healthcheck.py for endpoint-level checks (`/`, `/v1/models`, `/v1/chat/completions` dry smoke).
    Document exact examples in README and docs/DEPLOYMENT.md, using Thai-first operator messages in command outputs.
  </action>
  <verify>
    - python scripts/ops.py --help
    - python scripts/ops.py health --mode local
    - python scripts/ops.py health --mode docker
    - rg "python scripts/ops.py" README.md docs/DEPLOYMENT.md
  </verify>
  <done>
    Operators use one command family for restart, health, and logs across deployment modes.
  </done>
  <acceptance_criteria>
    `python scripts/ops.py --help` lists subcommands `restart`, `health`, `logs`; `python scripts/ops.py health --mode local` and `python scripts/ops.py health --mode docker` both return exit code `0` on healthy runtime; `README.md` and `docs/DEPLOYMENT.md` each contain an `Ops Commands` section with the exact examples `python scripts/ops.py restart --mode local`, `python scripts/ops.py health --mode docker`, and `python scripts/ops.py logs --mode docker --lines 50`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- python scripts/run_local.py --preflight-only
- docker compose up -d && docker compose ps
- python scripts/ops.py health --mode docker
- python scripts/ops.py logs --mode docker --lines 50
- bash scripts/coolify_verify.sh https://$DOMAIN_PROXY
</verification>

<success_criteria>
- DEP-01, DEP-02, DEP-03 met through executable and documented runbooks.
- OPS-01 met through one operational command surface for restart/health/logs.
</success_criteria>
